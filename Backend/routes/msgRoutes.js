const express = require('express');
const authMiddleware = require('../middleware/authMiddleware.js');
const { } = require('../controller/msgController.js');

const msgRoute = express.Router();


module.exports = msgRoute;